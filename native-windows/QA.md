# 验证记录 — Windows 原生版 alpha 0.1

2026-09-06。原生版源码和输出均位于 `native-windows/`。原生开发验收阶段没有修改浏览器版或用户项目。GitHub 整理阶段另加固定的 `examples/reference` 与示例复制脚本；用户正在编辑的 `examples/sample` 保留在本地且不提交。

## 已完成

| 检查 | 结果 |
| --- | --- |
| Mac ARM64，.NET SDK 10.0.400 编译 Core / Checks | 0 warning / 0 error |
| Windows Forms x64 交叉编译、self-contained 发布 | 成功；包含 .NET / Windows Desktop 10.0.11 运行时 |
| 核心检查程序 | 28 项通过 |
| 微软 Open XML SDK 3.3.0 验证 | 合成测试和当前项目副本的 Excel 均无格式错误 |
| 原浏览器版 Zod / YAML 反向读取 | 桌面版重写的 5 个用例与原值深度比较一致 |
| ExcelJS 4.4.0 独立回读 | 6 sheets、5 images、849 个字符串单元格；00012 保留 |
| 列宽 / 图片 / 页面设置 | 6/36/32/32/8/14；图片锚定 B 列、尺寸有效且不超过设定宽度；A4 横向、横 1 页 |
| ZIP / manifest | 文件字节、SHA-256、目录内文件与 ZIP 内文件一致；ZIP 不包含自身 |
| 不输出 HTML | 原生版导出目录和交付 ZIP 均无 HTML 报告 |
| 演示项目 | 独立的一用例、两步骤、截图 / SQL + CSV / 日志；成功导出并通过 Open XML 校验 |

28 项检查覆盖：双开锁、保留 ID、路径越界、大小写 ID 冲突、日文和日期、空字段、未知 YAML 字段拒绝、重复键、判定规则、步骤引用、文件 revision、CSV / TSV 引号与换行、UTF-8、SQL 元数据、证据篡改、删除恢复与递增 ID、无效步骤恢复为共通、原图保留 / 重置、快照与后续修改隔离、完整导出、数式注入防止、无 HTML、官方格式验证、失败清理、原图哈希、锁释放、`.yml` 保持和符号链接拒绝。

独立回读脚本：`tests/interop/verify.ts`。它使用父目录原有 ExcelJS 和数据读取器，仅用于额外验证，不属于桌面版运行或独立构建依赖。测试专用 Open XML SDK 也不进入运行包。

演示截图复用仓库原有的架空登录页面截图；没有打包用户新增的用例、截图或日志。原项目验证在临时目录副本中进行。

## 修复过并复测的问题

- OOXML 关系类型由命名空间对象拼接导致非法 XML 名称，改为 URI 字符串。
- 字体元素顺序不符合 Open XML，修正后官方验证通过。
- ExcelJS 解析依赖绘图命名空间前缀及图片文件名形式，采用常见的 `xdr` / `a` / `r` 和纯字母数字资源名。
- 原生 UI 的草稿丢弃会重新载入磁盘内容，避免后续取消操作留下错误的“已保存”状态。
- SplitContainer 初始化先设置尺寸再设分隔位置，避免默认小尺寸与分隔距离冲突。

## 未完成的实机验证

这是**编译和数据层验证通过的 Windows alpha**，不是已通过 Windows 实机验收的正式发行版。当前机器是 Mac，没有运行 Windows Forms 窗口，也没有用网页截图替代原生 UI 测试。

尚需 Windows 验证：双击启动、真实窗口布局、IME、RDP 剪贴板 / 拖拽、DPI、多显示器、图片注释绘制与裁剪、实际 Excel 的打印缩放与超链接、公司允许列表和离线环境执行。完整清单见 `WINDOWS-ACCEPTANCE.md`。

WebP、用例删除、恢复列表、Runs、遮罩、Shift-JIS、自动采集、自动更新、签名和安装器尚未实现。
