# Windows 実機受け入れチェック

状態：未実施。Mac でのクロスコンパイル・Core テストを、Windows 実機での確認の代わりにはしません。

最初は本番証拠ではなく、プロジェクトのコピーを使用してください。テスト日時、Windows バージョン、CPU、表示倍率、RDP クライアント、結果・不具合を記録してください。

| 操作 | 期待結果 | 実機結果 |
| --- | --- | --- |
| 配布 ZIP 展開 → evikit.exe | Bun / .NET / ブラウザの追加インストールなしで起動 | 未確認 |
| ブラウザ無効の RDP 内で起動 | ネイティブウィンドウを表示。ブラウザを起動しない | 未確認 |
| 日本語パスのフォルダーを開く | project.yaml の表示・編集が可能 | 未確認 |
| 用例追加、ステップ・判定を入力、保存、再起動 | 入力値・判定・関連証拠が保持される | 未確認 |
| IME 入力、長い文章、100/150/200% DPI | 操作ボタン・入力欄が利用可能 | 未確認 |
| 未保存で用例切り替え・終了 | 保存 / 破棄 / キャンセルが正しく動作 | 未確認 |
| ステップ上下移動・削除 | 証拠との関連が維持され、削除時は共通になる | 未確認 |
| テスト条件の列・複数行ダイアログで入力 → 保存 → 再起動 | Step ごとの条件・改行・00012 を保持し、用例の前提条件と独立 | 未確認 |
| 条件付きステップの移動・削除 → Excel 出力 | 条件が元のステップに追従し、Excel では該当ステップ直下に表示 | 未確認 |
| RDP 内でスクリーンショットをコピー → 貼り付け | PNG 証拠を追加。RDP 間転送は会社設定次第 | 未確認 |
| 連続スクリーンショット → Step 選択 → Ctrl+Alt+S で 10 枚 | 入力ダイアログなしで 10 枚保存・採番。用例 / Step が正しい | 未確認 |
| Ctrl+Alt+R で同じ範囲を連続撮影 | 範囲選択なしで再撮影。evikit 自身が写らず、元アプリに操作を続けられる | 未確認 |
| 範囲選択を Esc / 右クリックで取消 | PNG・証拠を作らず撮影待機へ戻る | 未確認 |
| キーを押し続ける / 保存中に連打 | 同一押下の繰り返し・並行保存が発生しない | 未確認 |
| 一時停止 → 再開 → 次の Step | 停止中のショートカットは解除され、再開後は指定した Step に保存 | 未確認 |
| ショートカットが他アプリと競合 | 通知して代替キーまたはボタンで撮影可能 | 未確認 |
| 左側モニター（負座標）/ 100・150・200% 混在 DPI | 枠と保存画像の範囲が一致。画面構成変更後は再選択 | 未確認 |
| AWS Windows RDP 内の内蔵撮影（クリップボード転送なし） | 表示中のリモート画面を保存。ローカル PC の画面は対象外 | 未確認 |
| 保存先への書き込み失敗 → 復旧 → 再試行 | 次の撮影を止め、同じ画像を二重登録せず保存 | 未確認 |
| 保存前の回収記録が残った状態で再起動 → 回収 | 元の采集時間で追加。YAML に既にある画像は二重登録しない | 未確認 |
| 終了 → 縮略図一覧 → 見出し・説明・順序を編集 | 保存後と再起動後の順序・説明が一致 | 未確認 |
| 画像を複数選択 → 説明一括追記 / Step 一括設定 | 選択画像だけ更新。既存説明を保持して追記 | 未確認 |
| 整理画面で注釈・削除・復元・未保存終了 | 原図保持、復元可能、説明の保存 / 破棄 / 取消が正しい | 未確認 |
| 同一 Step の画像 10 枚を Excel に出力 | 10 枚と各説明が整理した順番に並ぶ | 未確認 |
| MP4 / MOV 等を追加、説明・Step を指定 | 2 GiB 以下の file 証拠として保存。原バイト・名前・SHA-256 保持 | 未確認 |
| 25 MiB 超の動画を取込・取り出し・出力 | UI が描画を継続し、動画をメモリに丸ごと展開しない | 未確認 |
| 動画を開く → 許可済みプレーヤー exe を選択 | ハッシュ確認後、そのプレーヤーで開く。自動でブラウザを起動しない | 未確認 |
| 動画の確認画像 → PNG 選択 → 00:35 | 動画と同じ Step に追加し、source に動画 ID と時間点を保存 | 未確認 |
| ZIP 展開 → Excel の動画リンク | 同梱ファイルをローカルの関連付け先プレーヤーで開く | 未確認 |
| 動画の書換え後に再生・出力 | SHA-256 不一致で停止し、残欠の交付物を公開しない | 未確認 |
| エクスプローラーからファイルをドロップ | 確認ダイアログ後に追加 | 未確認 |
| Excel のセル 00012 をコピー → 貼り付け | 表として保存され、00012 が保持される | 未確認 |
| 画像に枠・矢印・番号・文字・切り抜き、保存、再編集 | 原図が保持され、注釈の位置と切り抜きが一致 | 未確認 |
| 証拠削除 → 新規追加 → 復元 | ID を再利用せず、両方の証拠が残る | 未確認 |
| YAML を外部編集後、保存 | 競合を表示。古い画面で上書きしない | 未確認 |
| ブラウザ版で同じプロジェクトを開いたまま起動 | 使用中として拒否 | 未確認 |
| Excel・添付・ZIP 出力 | 日時フォルダー、全添付、SHA-256 一致 | 未確認 |
| 実 Excel で開く・印刷プレビュー | 修復警告なし、画像・改行・判定・縮尺を確認 | 未確認 |
| Excel のサマリ / 添付リンク | ZIP 展開後の相対パスで参照可能 | 未確認 |
| ネットワーク無効で一連の操作 | 起動・編集・注釈・出力が完了 | 未確認 |

Mac で HTML を表示できても Windows Forms の画面テストにはなりません。承認済み Windows 環境で上表を確認した後、利用範囲を広げてください。
